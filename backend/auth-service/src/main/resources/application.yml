server:
  port: 8086

spring:
  application:
    name: auth-service

  # 数据源配置
  datasource:
    driver-class-name: org.postgresql.Driver
    url: jdbc:postgresql://localhost:5432/redteam_file?useUnicode=true&characterEncoding=utf8&serverTimezone=Asia/Shanghai
    username: postgres
    password: postgres
    type: com.alibaba.druid.pool.DruidDataSource
    druid:
      initial-size: 5
      min-idle: 10
      max-active: 50

  # Redis配置
  data:
    redis:
      host: localhost
      port: 6379
      database: 0
      timeout: 10000ms

# MyBatis Plus配置
mybatis-plus:
  mapper-locations: classpath*:/mapper/**/*.xml
  type-aliases-package: com.redteam.auth.entity
  configuration:
    map-underscore-to-camel-case: true
    log-impl: org.apache.ibatis.logging.slf4j.Slf4jImpl

# Knife4j配置
knife4j:
  enable: true
  setting:
    language: zh_cn

# 日志配置
logging:
  level:
    root: info
    com.redteam: debug

# 安全配置
security:
  jwt:
    secret: redteam-file-platform-secret-key-for-jwt-token-generation-2024
    expiration: 86400000
    token-prefix: Bearer
    header-name: Authorization
  ignore-urls:
    - /auth/login
    - /auth/register
    - /auth/captcha
    - /auth/mfa/verify
    - /doc.html
    - /swagger-resources/**
    - /v3/api-docs/**
    - /webjars/**

# 国密算法密钥配置（v2.3 新增）
# 生产环境必须配置固定密钥，留空时启动会自动生成临时密钥（仅开发/测试）
redteam:
  crypto:
    # SM4 对称密钥（Base64，16 字节）。留空则启动时自动生成
    sm4-key:
    # SM2 私钥（Base64 PKCS#8）。留空则启动时自动生成
    sm2-private-key:
    # SM2 公钥（Base64 X.509）。留空则启动时自动生成
    sm2-public-key:
