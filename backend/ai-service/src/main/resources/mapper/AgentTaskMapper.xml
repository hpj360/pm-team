<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.redteam.ai.mapper.AgentTaskMapper">

    <select id="selectByUserId" resultType="com.redteam.ai.entity.AgentTaskEntity">
        SELECT task_id, query, user_id, status, conclusion, evidence_chain_json,
               referenced_files_json, confidence, traces_json, error_message,
               created_at, completed_at
        FROM ai_agent_task
        WHERE user_id = #{userId}
        ORDER BY created_at DESC
        LIMIT #{limit}
    </select>

</mapper>
